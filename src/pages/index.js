import { Helmet } from "react-helmet";

const HomePage = () => {
    return (
        <>
            <Helmet>
                <title>Home Page</title>
            </Helmet>
        </>
    );
};

export default HomePage;
